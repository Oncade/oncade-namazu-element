jdk:
  - openjdk21

before_install:
  - sdk install maven 3.9.9
  - sdk use maven 3.9.9
  - git clone --depth 1 --branch 3.7.17 https://github.com/NamazuStudios/elements.git /tmp/elements-sdk
  - sed -i 's|<module>web-ui-react-jetty</module>||g' /tmp/elements-sdk/pom.xml
  - sed -i 's|<module>archetype</module>||g' /tmp/elements-sdk/pom.xml
  - sed -i 's|<module>archetype-kotlin</module>||g' /tmp/elements-sdk/pom.xml
  - cd /tmp/elements-sdk && mvn install -DskipTests -Dmaven.javadoc.skip=true -Dmaven.source.skip=true -q

install:
  - mvn clean install -DskipTests -Dmaven.javadoc.skip=true
